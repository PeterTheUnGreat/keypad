##!/bin/bash

echo "Cleaning up"
echo "Write to target"

if [ $1 = m328pb ]
then
	avrdude -P /dev/spidev0.0:/dev/gpiochip0 -c linuxspi -p m328pb -U lfuse:r:-:h -U hfuse:r:-:h  -U efuse:r:-:h
else
	avrdude -P /dev/spidev0.0:/dev/gpiochip0 -c linuxspi -p m8 -U lfuse:r:-:h -U hfuse:r:-:h
fi
